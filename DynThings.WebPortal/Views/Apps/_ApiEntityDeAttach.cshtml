@model DynThings.Data.Models.AppAPIEntity

@using (Html.BeginForm("AppApiEntityDeAttachPV", "Apps", FormMethod.Post, new { id = "AppApiEntityDeAttachForm" }))
{
    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="myModalLabel">Deattach Confirmation</h4>
    </div>
    <div class="modal-body">

        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ID)
        @Html.HiddenFor(model => model.AppID)
        @Html.HiddenFor(model => model.SystemEntityID)
        <span>Are you sure you want to deattach the following object from @Model.App.Title app?</span><br /><br />
        <span>Type: System Entity</span><br />
        <span>Title: @Model.SystemEntity.Title</span><br />

    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        <button type="submit" class="btn btn-danger">Deattach</button>
    </div>
}

<script>
    //Attach : Delete Form Submit
    $(document).ready(function () {
        $("#AppApiEntityDeAttachForm").on("submit", function (event) {
            event.preventDefault();
            var url = $(this).attr("action");
            var formData = $(this).serialize();
            $.ajax({
                url: url,
                type: "POST",
                data: formData,
                dataType: "json",
                success: function (resp) {
                    LoadPart_AppApiEntitysByAppIDDiv(selectedAppID);
                }
            })
            LoadPart_AppApiEntitysByAppIDDiv(selectedAppID);
            $('#mdl').modal('hide');
        });
    });
</script>

